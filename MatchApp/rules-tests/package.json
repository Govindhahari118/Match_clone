{
  "name": "matchapp-rules-tests",
  "private": true,
  "type": "module",
  "engines": {
    "node": "22"
  },
  "scripts": {
    "test": "firebase emulators:exec --project matchapp-rules-test --only firestore,storage \"node --test --test-concurrency=1 *.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.0"
  }
}