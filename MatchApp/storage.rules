rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    function signedIn() { return request.auth != null; }
    match /photos/{uid}/{allPaths=**} {
      allow read: if signedIn();
      allow create, update: if signedIn() && request.auth.uid == uid && request.resource.size > 0 && request.resource.size < 2 * 1024 * 1024 && request.resource.contentType.matches('image/.*');
      allow delete: if signedIn() && request.auth.uid == uid;
    }
    match /videos/{uid}/{allPaths=**} {
      allow read: if signedIn();
      allow create, update: if signedIn() && request.auth.uid == uid && request.resource.size > 0 && request.resource.size < 50 * 1024 * 1024 && request.resource.contentType.matches('video/.*');
      allow delete: if signedIn() && request.auth.uid == uid;
    }
    match /voicebios/{uid}/{allPaths=**} {
      allow read: if signedIn();
      allow create, update: if signedIn() && request.auth.uid == uid && request.resource.size > 0 && request.resource.size < 5 * 1024 * 1024 && request.resource.contentType.matches('audio/.*');
      allow delete: if signedIn() && request.auth.uid == uid;
    }
    match /verifications/{uid}/{allPaths=**} {
      allow read: if false;
      allow create, update: if signedIn() && request.auth.uid == uid && request.resource.size > 0 && request.resource.size < 5 * 1024 * 1024 && (request.resource.contentType.matches('image/.*') || request.resource.contentType.matches('application/pdf'));
      allow delete: if false;
    }
    match /chat-media/{threadId}/{fileName} {
      allow read: if signedIn() && (request.auth.uid == resource.metadata.senderUid || request.auth.uid == resource.metadata.recipientUid);
      allow create, update: if signedIn() && request.auth.uid == request.resource.metadata.senderUid && request.resource.metadata.recipientUid is string && request.resource.metadata.recipientUid != request.auth.uid && request.resource.metadata.threadId == threadId && request.resource.size > 0 && request.resource.size < 12 * 1024 * 1024 && (request.resource.contentType.matches('image/.*') || request.resource.contentType.matches('audio/.*'));
      allow delete: if signedIn() && request.auth.uid == resource.metadata.senderUid;
    }
  }
}
