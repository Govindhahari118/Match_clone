# Branch Scope

This branch hardens the existing Next.js + Express + PostgreSQL matrimony platform. It does not claim a completed native Android implementation; the repository's `mobile/` directory remains a placeholder. The branch is intended to be merged only after CI and migration gates are green.
